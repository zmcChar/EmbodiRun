# Documentation build dependencies for ReadTheDocs.
mkdocs-material>=9.5,<10
# API reference is generated by Griffe static analysis, so the package
# itself is not installed for the docs build.
mkdocstrings[python]>=1.0,<2
# mkdocstrings wraps long signatures with ruff when it is available.
ruff>=0.16,<1
