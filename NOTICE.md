RLinf Deploy

This repository contains robot and simulator deployment software. It does not
vendor or execute model implementations.

The optional `third_party/vvla` Git submodule points to RLinf Inference and is
licensed under its own repository terms. Franka support uses the separately
installed `franky-control` package from the Franky project.
