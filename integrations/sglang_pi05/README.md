# RLinf Deploy SGLang Pi05 integration

This optional package owns the LeRobot Pi05 checkpoint compatibility pipeline.
Install it only in the managed SGLang inference environment. The managed
entry point is `rlinf-sglang-pi05-serve`; Deploy's historical
`rlinf-sglang-serve` launcher remains a thin compatibility wrapper in the root
package.
